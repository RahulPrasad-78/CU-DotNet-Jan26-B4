**Day - 17 \& 18**



Array.sort(arr);

Array.Reverse(arr);



* Inheritance is a relationship between class.
* Always the Parent class Constructor is called first if we call the Constructor without arugments
* :base is used we call the derived constructor. it will call the base class.
* In override of ToString() method we call base class override in derived class so we can by using(base.ToString)
* Base class collection can have all type of array



---



**Code To Improve**

* Dynamically should call the method of the class for which object belongs

 	create virtual method in base class with implementation

 	and override in derived class

 	benefit: Dynamic binding/ runtime Polymorphism

 

* We should not be allowed to create object of base class

 	create abstract class

 	Benefits: class not instantiable must be inherited to be concrete class



What is Abstract Class?

 	can have abstract method. (without implementation)



* base class method may not have any implementations sometime

 	Make the Method as Abstract wit ";" after signature.

 	Benefits: it is a contract to be implemented by every class method



* This method will be used as a contract so that every derived class must implement it with different business logic.





Contract - abstract class or interface

 	abstract class can have some non abstract methods interface will have all non implemented 	method



 	how to create an interface?

 		Start name with "I"

 		interface keyword

 			e.g IprocessPayment

 		Interface keyword



Interface - for contracts

A class can be derived from a class multiple interface

example of any built-in interface in .NET



IEnumerable           IEnumerator



collections implement this interface

Generic Collections    -   same type elements



why generic collections when Array is there?

* array is fixed size, collections are growable
* Generic Collection class<Type>



NON-GENERIC COLLECTION(objects)

* ArrayList - any types of values - objects
* all types are converted to object(boxing) and reverse(Unboxing).
* can add anything or any type of value. System.Collections;
