1. Create a Collections of words
2. pick one word by using Random class.
3. create a array to take input from user and first fill all places with "\_".
4. define the variables like lives.
5. run a loop while lives is >=0;
6. take input from user in char datatype(take only char otherwise give msg).
7. create one more hashSet and fill every place with 0.
8. take input and fill 1 in the arr flag. convert it into uppercase
9. in that check every element from original world and fill the array if exist
10. if user put a number or special character dont remove live
11. if user put repeated char. checck it from arr and don't remove the live
12. if not remove the live--;
13. also check if array have any "\_" left or not.
14. if no "\_" left then user win...
15. if "\_" is left and user don't have live. user lost
