ArrayList - Non generic collections
Collection are allocated to 0 
First item added it is allocated to 4 if more than capacity.. the capacity will become double

In ArrayList filter or check int items?

on Collection Count is a property.
Count() is a method provided by LINQ(Language integrated Query).
In LINQ we have a Collection of many extension method
Count() is a extension method from LINQ

What is a Query? That can Handle select where orderBy GroupBy
Query works on Database tables
Microsoft added LINQ to provide same functionality on collections in a very effective way using some built in methods.

Lambda expression
It is a way to write methods in a compact way

x => x%2 ==1
static bool isOdd(int x){
	if(x%2 ==1)	return true;
	
	return false;
}

Array is a collection
Is every collection an array? No...

What is an Extension method ?
Can we create our own extension method? yes
why to create?
in String Class - is a sealed class
do we have any method to get number of words in a string? NO
can I add this method In String Class?  NO

I want to add this wordCount method in String Class.

In this case we can add an extension method to the string class.

Extension method is static method written in static class


How to create - 
Benefits of LINQ - many reusable methods are available without writing detailed code,
and methods can be chained


HashSet - can not have duplicate values - np exception
vs List<T> can have duplicate value

HashTable(non - generic) vs Dictionary(Generic)