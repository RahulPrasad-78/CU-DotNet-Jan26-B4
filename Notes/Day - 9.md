**Day - 9**



Method:

Main() is entry point method



(access specifier) (static) (return type) NameofFunction(parameters)

{



}



default access specifier is Internal for class

default specifier for method is Private



parameters and arguments

arguments will be passed when calling the methods



value type are passed to a method as value type

and ref  type are passed as ref type;

